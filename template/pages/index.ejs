<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="<%= asset('/css/style.css') %>">
    <link rel="icon" type="image/x-icon" href="<%= asset('/img/favicon.png') %>">
    <title>我的博客</title>
</head>

<body>
    <div class="container">
        <h1>我的博客</h1>
        <div class="posts-list">
            <% for(let post of posts) { %>
                <div class="post-item">
                    <h3><a href="<%=post.dest%>">
                            <%=post.title %>
                        </a>
                    </h3>
                    <div class="post-meta">
                        <time><%=post.creationDate.toLocaleDateString("zh-CN")%></time>
                        <div class="post-tags">
                            <% for(let tag of post.tags) { %>
                                <span style="background-color: <%= tag.color %>; color:#FFF">
                                    <%=tag.title%>
                                </span>
                                <% } %>
                        </div>
                    </div>
                </div>
                <% } %>
        </div>
    </div>
    <div>Powered by miscc version <%= MISCC_VERSION%>
    </div>
</body>

</html>